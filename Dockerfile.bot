FROM python:3.11-slim
WORKDIR /app
# Create data directory
RUN mkdir -p /data
# Set environment variable for the bot to use the persistent volume path
ENV DATA_DIR=/data
COPY bot.py .
RUN pip install --no-cache-dir discord.py aiohttp certifi
CMD ["python", "bot.py"]
